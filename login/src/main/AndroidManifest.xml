<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.daily.news.login">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <meta-data
            android:name="WOA_APP_ID"
            android:value="416" />

        <activity
            android:name="com.daily.news.login.LoginActivity"
            android:launchMode="singleTask"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="@string/data_scheme" />

                <data android:path="/login/LoginActivity" />

                <!--以下测试 host -->
                <data android:host="@string/data_host1" />
                <data android:host="@string/data_host" />

            </intent-filter>

        </activity>

        <activity
            android:name="com.daily.news.login.zbtxz.ZBLoginActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustPan|stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="@string/data_scheme" />

                <data android:path="/login/ZBLoginActivity" />

                <!--以下测试 host -->
                <data android:host="@string/data_host1" />
                <data android:host="@string/data_host" />
            </intent-filter>

        </activity>
        <activity
            android:name="com.daily.news.login.zbtxz.ZBRegisterActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="@string/data_scheme" />

                <data android:scheme="@string/data_scheme" />
                <data android:path="/login/ZBRegisterActivity" />

                <!--以下测试 host -->
                <data android:host="@string/data_host1" />
                <data android:host="@string/data_host" />

            </intent-filter>
        </activity>
        <activity
            android:name="com.daily.news.login.zbtxz.ZBResetNewPassWord"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="@string/data_scheme" />
                <data android:path="/login/ZBResetNewPassWord" />

                <!--以下测试 host -->
                <data android:host="@string/data_host1" />
                <data android:host="@string/data_host" />

            </intent-filter>
        </activity>
        <activity
            android:name="com.daily.news.login.zbtxz.ZBResetPWSmsLogin"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="@string/data_scheme" />
                <data android:path="/login/ZBResetPWSmsLogin" />

                <!--以下测试 host -->
                <data android:host="@string/data_host1" />
                <data android:host="@string/data_host" />

            </intent-filter>
        </activity>
        <activity
            android:name="com.daily.news.login.zbtxz.ZBVerificationActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="@string/data_scheme" />
                <data android:path="/login/ZBVerificationActivity" />

                <!--以下测试 host -->
                <data android:host="@string/data_host1" />
                <data android:host="@string/data_host" />

            </intent-filter>
        </activity>

        <activity
            android:name="com.daily.news.login.zbtxz.ZBMobileValidateActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="@string/data_scheme" />
                <data android:path="/login/ZBMobileValidateActivity" />

                <!--以下测试 host -->
                <data android:host="@string/data_host1" />
                <data android:host="@string/data_host" />

            </intent-filter>
        </activity>

    </application>

</manifest>