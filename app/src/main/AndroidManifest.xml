<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.login">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="ZBP_APP_ID"
            android:value="1"/>
        <meta-data
            android:name="ZBP_APP_KEY"
            android:value="fQNZBzR9ej"/>
        <meta-data
            android:name="ZBP_APP_SECRET"
            android:value="E1v9TSMLLit2VHTTl0i9"/>
        <meta-data
            android:name="ZBP_APP_ENV"
            android:value="dev"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 边锋SDK ID -->
        <meta-data
            android:name="WOA_APP_ID"
            android:value="416" />

        <activity android:name=".TestLoginActivity" />
        <activity android:name=".tesk.TestAuthActivity"></activity>
    </application>

</manifest>